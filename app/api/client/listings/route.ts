import {NextResponse} from "next/server";import {createOwnedListing,listOwnedListings} from "../../../../lib/marketplace-repository";import {parseCreateListingInput} from "../../../../lib/marketplace";import {errorMessage,errorStatus,noStoreHeaders,readJson} from "../../../../lib/api";
export async function GET(){try{return NextResponse.json({listings:await listOwnedListings()},{headers:noStoreHeaders()})}catch{return NextResponse.json({error:"Unauthorized"},{status:401,headers:noStoreHeaders()})}}
export async function POST(request:Request){try{const input=parseCreateListingInput(await readJson(request));return NextResponse.json({listing:await createOwnedListing(input)},{status:201,headers:noStoreHeaders()})}catch(error){const message=errorMessage(error);return NextResponse.json({error:message},{status:errorStatus(message),headers:noStoreHeaders()})}}