"use client";import {FormEvent,useEffect,useState} from "react";import {useParams,useRouter} from "next/navigation";import Link from "next/link";import {apiJson} from "../../../../lib/client-api";type Listing={title:string;description:string;category:string;priceCents:number|null;status:string};
export default function ManageListingPage(){const{id}=useParams<{id:string}>(),router=useRouter();const[listing,setListing]=useState<Listing|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{const controller=new AbortController();(async()=>{try{const r=await fetch(`/api/client/listings/${id}`,{signal:controller.signal,cache:"no-store"});const b=await apiJson(r) as {listing?:Listing;error?:string}|null;if(!r.ok||!b?.listing)throw new Error(b?.error??"Could not load listing.");setListing(b.listing)}catch(e){if(e instanceof DOMException&&e.name==="AbortError")return;setError(e instanceof Error?e.message:"Could not load listing.")}})();return()=>controller.abort()},[id]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;setBusy(true);setError("");try{const d=new FormData(e.currentTarget),dollars=String(d.get("price")??"").trim();const r=await fetch(`/api/client/listings/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:d.get("title"),description:d.get("description"),category:d.get("category"),status:d.get("status"),priceCents:dollars===""?null:Math.round(Number(dollars)*100)})});const b=await apiJson(r) as {error?:string}|null;if(!r.ok)throw new Error(b?.error??"Could not save listing.");router.push("/client");router.refresh()}catch(e){setError(e instanceof Error?e.message:"Could not save listing.");setBusy(false)}}async function remove(){if(busy||!confirm("Delete this listing? This cannot be undone."))return;setBusy(true);setError("");try{const r=await fetch(`/api/client/listings/${id}`,{method:"DELETE"});if(!r.ok){const b=await apiJson(r) as {error?:string}|null;throw new Error(b?.error??"Could not delete listing.")}router.push("/client");router.refresh()}catch(e){setError(e instanceof Error?e.message:"Could not delete listing.");setBusy(false)}}if(error&&!listing)return <main className="shell"><section className="form-card"><h1>Listing unavailable</h1><p className="form-error">{error}</p><Link className="button" href="/client">Back to my hustle</Link></section></main>;if(!listing)return <main className="shell"><section className="form-card"><p aria-live="polite">Loading listing…</p></section></main>;return <main className="shell marketplace-shell"><section className="form-card"><p className="eyebrow">Hustle First™ Marketplace</p><h1>Manage listing</h1><form className="listing-form" onSubmit={save} aria-busy={busy}><label>Title<input name="title" required maxLength={120} defaultValue={listing.title}/></label><label>Category<select name="category" defaultValue={listing.category}><option value="services">Service</option><option value="goods">Goods</option><option value="rides">Ride</option><option value="jobs">Job opportunity</option><option value="other">Other</option></select></label><label>Description<textarea name="description" required maxLength={4000} rows={7} defaultValue={listing.description}/></label><label>Price in dollars <span className="optional">(optional)</span><input name="price" type="number" min="0" max="1000000" step="0.01" inputMode="decimal" defaultValue={listing.priceCents===null?"":(listing.priceCents/100).toFixed(2)}/></label><label>Status<select name="status" defaultValue={listing.status}><option value="active">Active</option><option value="paused">Paused</option><option value="closed">Closed</option></select></label>{error&&<p className="form-error" role="alert">{error}</p>}<div className="form-actions"><button className="button" disabled={busy} type="submit">{busy?"Saving…":"Save changes"}</button><Link className="button secondary" href="/client">Cancel</Link><button className="danger-button" disabled={busy} type="button" onClick={remove}>Delete listing</button></div></form></section><footer>Hustle First™ · Cactus🌵Byte Studios™ · All Rights Reserved.</footer></main>}